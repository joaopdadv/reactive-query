{
  "name": "reactive-query-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build-core": "npm run -w packages/core build",
    "dev-core": "npm run -w packages/core dev",
    "test-core": "npm run -w packages/core test"
  }
}

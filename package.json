{
  "name": "reactive-query-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -w packages/lib build",
    "dev": "npm run -w packages/lib dev",
    "test": "npm run -w packages/lib test"
  }
}
